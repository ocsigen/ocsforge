touch .depend
echo "DBUSER=ocsimore" > Makefile.config
echo "PAM=YES" >> Makefile.config
echo "PASSWORD=$1" >> Makefile.config
echo "STATICDIR :=/var/www/ocsimore" >> Makefile.config
echo "USER := www-data" >> Makefile.config
echo "GROUP := www-data" >> Makefile.config
echo "SRC := "`pwd` >> Makefile.config
mkdir -p etc
mkdir -p etc/ocsigen
mkdir -p var
mkdir -p var/lib
mkdir -p var/run
mkdir -p var/log
echo "$1" > etc/ocsigen/ocsimorepassword
